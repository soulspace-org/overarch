(ns org.soulspace.overarch.adapter.render.markdown-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.adapter.render.markdown :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))