(ns org.soulspace.overarch.adapter.render.graphviz-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.adapter.render.graphviz :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))