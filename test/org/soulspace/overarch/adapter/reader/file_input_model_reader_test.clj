(ns org.soulspace.overarch.adapter.reader.file-input-model-reader-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.adapter.reader.file-input-model-reader :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))