(ns org.soulspace.overarch.util.io-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.util.io :refer :all]))


(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))

