(ns org.soulspace.overarch.application.export-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.application.export :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))

