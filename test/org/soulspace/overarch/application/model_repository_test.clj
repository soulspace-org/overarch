(ns org.soulspace.overarch.application.model-repository-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.application.model-repository :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))