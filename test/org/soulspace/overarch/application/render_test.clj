(ns org.soulspace.overarch.application.render-test
  (:require [clojure.test :refer :all]
            [org.soulspace.overarch.application.render :refer :all]))

(deftest compile-test
  (testing "Compilation"
    (is (= 1 1))))